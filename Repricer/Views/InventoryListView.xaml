<UserControl x:Class="Repricer.Views.InventoryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Repricer.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ProgressBar IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="ImportMF" Margin="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock>Import MF Inventory (</TextBlock>
                    <TextBlock Text="{Binding MFItemsCount}" />
                    <TextBlock>)</TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="ImportFBA" Margin="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock>Import FBA Inventory (</TextBlock>
                    <TextBlock Text="{Binding FBAItemsCount}" />
                    <TextBlock>)</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
        <mah:MetroTabControl Grid.Row="2">
            <mah:MetroTabItem Header="MFs">
                <DataGrid x:Name="MFInventoryItems"/>
            </mah:MetroTabItem>
            <mah:MetroTabItem Header="FBAs">
                <DataGrid x:Name="FBAInventoryItems"/>
            </mah:MetroTabItem>
        </mah:MetroTabControl>
    </Grid>
</UserControl>
