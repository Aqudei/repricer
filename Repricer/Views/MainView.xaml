<mah:MetroWindow
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" x:Class="Repricer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Repricer.Views"
        mc:Ignorable="d"
        Title="ASeller Repricer Tool" Height="600" Width="800" WindowState="Maximized">
    <mah:MetroWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </mah:MetroWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="ImportMF" Margin="1">Import MF Inventory</Button>
            <Button x:Name="ImportFBA" Margin="1">Import FBA Inventory</Button>
        </StackPanel>
        <ContentControl x:Name="ActiveItem" Grid.Row="1"/>
        <ProgressBar Grid.Row="2"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     IsIndeterminate="True"
                     Height="20"  />
    </Grid>
</mah:MetroWindow>
